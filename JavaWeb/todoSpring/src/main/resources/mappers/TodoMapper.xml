<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.bitcprac.todospring.mapper.TodoMapper">

    <select id="getNow" resultType="string">
        select now()
    </select>

    <insert id="insert">
        insert into tbl_todo(title, dueDate, writer)
        values(#{title}, #{dueDate}, #{writer})
    </insert>

    <select id="selectAll" resultType="org.bitcprac.todospring.domain.TodoVO">
        select * from tbl_todo
    </select>

    <select id="selectOne" resultType="org.bitcprac.todospring.domain.TodoVO">
        select * from tbl_todo where tno=#{tno}
    </select>

</mapper>